// 原生平台占位：Web 跳转页只在 web 构建中使用。
import 'package:flutter/material.dart';

class BlogRedirectPage extends StatelessWidget {
  const BlogRedirectPage({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
